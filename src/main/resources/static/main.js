// Add your JavaScript functions for interacting with the Java server via REST calls

function createUser() {
    // Implement the logic for creating a user by sending a POST request to the "/users" endpoint
}

function getMatchingUsers() {
    // Implement the logic for retrieving matching users by sending a GET request to the "/users/{userId}/matches" endpoint
}

function sendChatPrompt() {
    // Implement the logic for sending a chat prompt by sending a POST request to the "/users/{userId}/chat" endpoint
}

// Add event listeners to your HTML elements to call these functions when needed
